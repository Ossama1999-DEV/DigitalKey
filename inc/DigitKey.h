/**!
 * @file DigitKey.h
 * @author DBIBIH Oussama
 */

#ifndef DIGITKEY_H
#define DIGITKEY_H

#include <math.h>

#define size 20

/**
 * @brief Copies the version string into the provided buffer.
 * 
 * @param version A pointer to the buffer where the version string will be stored.
 */
void get_version(unsigned char *version);

#endif // DIGITKEY_H
